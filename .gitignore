# Scanner output — contains real work email content (senders, subjects, asks)
# from a mailbox whose messages carry a Flow Automotive confidentiality notice.
# Stays on the Mac that generated it; never committed.
followups_local.html

__pycache__/
*.pyc
.DS_Store
